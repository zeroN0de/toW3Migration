import { BaseAccount } from '@bnb-chain/greenfield-cosmos-types/cosmos/auth/v1beta1/auth';
import { IDeleteObjectMsg } from '../../../messages/greenfield/storage/deleteObject';
import { IRawTxInfo } from '../../../tx';
import { BaseTx, IBaseMsg } from '../../baseTx';
export declare class DelObjectTx extends BaseTx {
    readonly rpcUrl: string;
    readonly chainId: string;
    readonly txType: string;
    constructor(rpcUrl: string, chainId: string);
    signTx({ bucketName, objectName, from, sequence, accountNumber, gasLimit, denom, gasPrice, }: IBaseMsg & IDeleteObjectMsg): Promise<import("../../../sign").ISignature712>;
    getRawTxInfo({ bucketName, from, sequence, gasLimit, sign, pubKey, objectName, denom, gasPrice, }: IBaseMsg & IDeleteObjectMsg & {
        sign: string;
    } & {
        pubKey: BaseAccount['pubKey'];
    }): Promise<IRawTxInfo>;
    getSimulateBytes({ from, bucketName, objectName, }: Pick<IBaseMsg & IDeleteObjectMsg, 'from' | 'bucketName' | 'objectName'>): Uint8Array;
    getAuthInfoBytes({ sequence, pubKey, gasLimit, denom, gasPrice, }: Pick<IBaseMsg & IDeleteObjectMsg, 'denom' | 'sequence' | 'gasLimit' | 'gasPrice'> & {
        pubKey: BaseAccount['pubKey'];
    }): Uint8Array;
}
