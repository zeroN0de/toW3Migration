import { BaseAccount } from '@bnb-chain/greenfield-cosmos-types/cosmos/auth/v1beta1/auth';
import { BaseTx, IBaseMsg, IRawTxInfo, ISendMsg } from '../baseTx';
export declare const TYPE_URL = "/bnbchain.greenfield.bridge.MsgTransferOut";
export declare class TransferOutTx extends BaseTx {
    readonly rpcUrl: string;
    readonly chainId: string;
    readonly txType: string;
    constructor(rpcUrl: string, chainId: string);
    getRawTxInfo({ sequence, sign, pubKey, from, to, amount, denom, gasLimit, gasPrice, }: Pick<ISendMsg & IBaseMsg, 'sequence' | 'from' | 'to' | 'amount' | 'denom' | 'gasLimit' | 'gasPrice'> & {
        sign: string;
    } & {
        pubKey: BaseAccount['pubKey'];
    }): Promise<IRawTxInfo>;
    getSimulateBytes({ from, to, amount, denom, }: Pick<ISendMsg & IBaseMsg, 'from' | 'to' | 'amount' | 'denom'>): Uint8Array;
    getAuthInfoBytes({ sequence, pubKey, gasLimit, denom, gasPrice, }: Pick<ISendMsg & IBaseMsg, 'denom' | 'sequence' | 'gasLimit' | 'gasPrice'> & {
        pubKey: BaseAccount['pubKey'];
    }): Uint8Array;
    signTx({ from, to, sequence, accountNumber, amount, denom, gasLimit, gasPrice, }: ISendMsg & IBaseMsg): Promise<import("../..").ISignature712>;
    simulateRelayFee(): Promise<import("@bnb-chain/greenfield-cosmos-types/greenfield/bridge/query").QueryParamsResponse>;
}
