export declare class FileHandler {
    static getPieceHashRoots(bytes: Uint8Array, segmentSize?: number, dataBlocks?: number, parityBlocks?: number): Promise<{
        contentLength: number;
        expectCheckSums: any;
    }>;
}
